name: Patch PR44 CI compatibility

on:
  push:
    branches:
      - feature/full-pending-procurement-analysis-issue-43
    paths:
      - .github/workflows/patch-pr44-ci.yml

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/full-pending-procurement-analysis-issue-43
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - name: Apply compatibility patch
        run: python .github/patch_pr44_ci.py
      - name: Remove one-time patch files
        run: rm -f .github/patch_pr44_ci.py .github/workflows/patch-pr44-ci.yml
      - name: Commit patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Fix persistent analysis CI compatibility"
          git push origin HEAD:feature/full-pending-procurement-analysis-issue-43
