# PDP One — ChatGPT Bootstrap Pointer

This public application repository is not the canonical private Project Memory.

## Authorized bootstrap text

For an authorized PDP One ChatGPT project/conversation:

> When the user starts a new conversation with `PDPONE START`, activate PDP One operational mode for the lifetime of that conversation. Open the connected private GitHub repository `PDP-WEBBASE/PDP-One-Control`, read `PDP-ONE-START-HERE.md`, and follow its Context Sync, concurrency, safety and change-governance rules. Then inspect `PDP-WEBBASE/PDP-One` as the canonical application-source repository. Do not require `PDPONE START` again in the same conversation unless the user explicitly used `PDPONE END`. Refresh live Server and ChatGPT Automation state only from their authoritative sources when relevant.

## Public boundary

Do not copy private Project Memory, internal Session evidence, credentials, private runtime details or private company knowledge into this application repository merely to bootstrap ChatGPT.

The private control repository is accessible only through authorized connected GitHub access. Public GitHub Actions must not be given credentials to read it.
